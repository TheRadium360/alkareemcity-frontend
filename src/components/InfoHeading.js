import React from "react";

export default function (props) {
  return (
    
      <div>
        <span>{props.heading} <span className="fw-bold"> {props.value}</span></span>

      </div>
    
  );
}
